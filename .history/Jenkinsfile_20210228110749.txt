pipeline {

    stages {
        stage('Build') { 
            steps {
                if (isUnix()) {
                    sh "npm install"
                } else {
                    bat(/"${MAVEN_HOME}\bin\mvn" -Dmaven.test.failure.ignore clean package/)
                }
                sh 'npm install' 
            }
        }
    }
}