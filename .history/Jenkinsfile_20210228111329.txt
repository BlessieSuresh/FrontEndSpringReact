pipeline {

    stages {
        stage('Build') { 
            steps {
                if (isUnix()) {
                    sh "npm install"
                } else {
                    bat(/"npm install"/)
                }
                sh 'npm install' 
            }
        }
    }
}