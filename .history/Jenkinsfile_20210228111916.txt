#!/usr/bin/env groovy

node {
    
    stage('checkout') {
        checkout scm
    }

    stage('Build') {
        if (isUnix()) {
            sh "npm install"
        } else {
            bat(/"npm install"/)
        }
   }
}
